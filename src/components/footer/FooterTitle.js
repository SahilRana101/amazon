import React from "react";

const FooterTitle = ({ title }) => {
  return (
    <h2 className="text-white font-titleFont text-base font-semibold mb-3">
      {title}
    </h2>
  );
};

export default FooterTitle;
